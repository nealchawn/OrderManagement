<p id="notice"><%= notice %></p>

<style>
span.tab{
    padding: 0 80px; /* Or desired space*/
}

</style>
<center>
<table style="width:85%">
  <tr><th>############################################</th></tr>
  <tr><th><center>Order OverView </center></th></tr>
  <tr><th>############################################</th></tr>
</table>
<%   @orders.each do |o| 
       if (o.id == @top_order) %>
       <font color="goldenrod">
       <table style="width:85%">
        <tr><th>############################################</th></tr>
        <tr><th><center> This Order Made the most in Sales</center></th></tr>
      <% else %>
        <br>
        <table style="width:85%">
        <tr><th>############################################</th></tr>
      <% end%>

      <tr>
        <th>
          <center>
          <%= link_to "Order #{o.id}", order_path(o.id) %>
          </center>
        </th>
      </tr>
      <tr>
        <th>
          <center>
            Address <%= o.address %>
          </center>
        </th>
      </tr>
      <tr>
        <th>
          <center>
            Shipped On <%= o.shipped_on %>
          </center>
        </th>
      </tr>
      <tr>
        <th>
          <center>
            Order Total: <%= "#{o.total_price}" %>
          </center>
        </th>
      </tr> 
      <tr>
          <td><center><%= link_to 'Show this Order', o %>
         <%= link_to 'Edit this Order', edit_order_path(o) %>
        <%= link_to 'Destroy this Order', o, method: :delete, data: { confirm: 'Are you sure?' } %></center></td>
      </tr>
        <tr><th>############################################</th></tr>
      </table>
          <% if (o.id == @top_order) %>
        </font>
      <% end %>
     <% end %>
   </table>
   </center>

<%= link_to 'New Order', new_order_path %>

